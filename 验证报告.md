# Nacos 3.x 未授权权限绕过（XVE-2026-53002）本地复现与危害分析

| 项 | 内容 |
| --- | --- |
| 编号 | XVE-2026-53002 / CNNVD-2026-26637595 |
| 漏洞类型 | 身份鉴别错误（鉴权作用域错配） |
| 影响版本 | Nacos 3.0.0 – 3.2.3 |
| 修复版本 | 3.2.4 |
| 验证环境 | Nacos 3.2.3 官方 release，standalone + 内嵌 Derby，OpenJDK 21 |
| 验证日期 | 2026-09-04 |
| 验证结论 | **漏洞存在，未认证可完全接管服务端**（PoC 退出码 0） |

> 本报告中"实测结果"章节均为本地受控环境实际执行输出；"分析研判"章节标注了推断成分。所有截图取自本次实际执行过程，未做内容改写。

---

## 1. 环境搭建

| 项 | 值 |
| --- | --- |
| 版本 | 3.2.3（`/v3/console/server/state` 自报 `"version":"3.2.3"`） |
| 部署方式 | `bin/startup.sh -m standalone`，内嵌 Derby |
| 服务端口 | 8848（HTTP OPEN_API / ADMIN API，contextPath `/nacos`） |
| 控制台端口 | 8080（Console API，contextPath `/`） |
| 鉴权配置 | **全部保持出厂默认**，仅按首次启动的强制要求填入了 `token.secret.key` 与 `server.identity.*` |

关键的四个开关（`conf/application.properties` 出厂默认值，未做任何修改）：

```properties
nacos.core.auth.enabled=false          # OPEN_API 鉴权，出厂默认关闭
nacos.core.auth.admin.enabled=true     # /v3/admin/** 鉴权，出厂默认开启
nacos.core.auth.console.enabled=true   # /v3/console/** 鉴权，出厂默认开启
nacos.core.auth.caching.enabled=true   # 权限缓存，15s 刷新窗口
```

为贴近真实场景，环境中额外做了两件事：初始化真实管理员 `nacos`，并以管理员身份发布一份含数据库口令、Redis 口令、HMAC 密钥的生产配置 `payment-service.yaml`（Group `PROD`）。

**截图：** `00-环境确认.png`、`01~04-攻击前-*.png`

---

## 2. 实测结果

### 2.1 攻击前基线：控制台侧鉴权确实是生效的

同一个路径 `/v3/auth/user/list`，两个端口的行为完全相反：

| 入口 | 匿名请求结果 |
| --- | --- |
| `http://<host>:8080/v3/auth/user/list` （Console） | `HTTP 403` `access denied` |
| `http://<host>:8080/v3/console/cs/config?...` （Console） | `HTTP 403` `access denied` |
| `http://<host>:8848/nacos/v3/auth/user/list` （OPEN_API） | `HTTP 200` + **全部用户名与 bcrypt 口令哈希** |

这一点很关键：从管理员视角看，控制台需要登录、配置读不到、`server/state` 还回报 `auth_enabled=true`，"认证是开着的"这个判断在直觉上完全成立——但 8848 上的同名接口在裸奔。

**截图：** `11-步骤1-攻击前基线.png`

### 2.2 PoC 执行：未认证 → 全局管理员

对 `http://127.0.0.1:8848` 执行文档提供的 PoC，七步全部成功，退出码 0：

```
[1] 匿名读取用户列表成功: 1 个用户（含 bcrypt 哈希泄露）
[2] POST /v3/auth/user        -> 200 {"code":0,"data":"create user ok!"}
[3] POST /v3/auth/role        -> 200 {"code":0,"data":"add role ok!"}
[4] POST /v3/auth/permission  -> 200 {"code":0,"data":"add permission ok!"}
[5] 等待权限缓存刷新 20s ...
[6] POST /v3/auth/user/login  -> 200
[7] GET  /v3/admin/core/namespace/list -> 200 {"code":0,...}

[+] 漏洞存在，完全接管成功: 未认证 -> 全局管理员
```

整个链路无需任何凭证，唯一的"成本"是第 5 步等待权限缓存刷新的 15 秒窗口（`nacos.core.auth.caching.enabled=true` 导致）。

攻击完成后，在管理员控制台里可以直接看到攻击者留下的痕迹：用户列表多出 `poc_h7zhoiek7z`，角色管理多出一条绑定，权限管理出现一条 **资源 `*` / 动作 `读写`** 的记录。

**截图：** `12-步骤2-PoC执行.png`、`05~07-攻击后-*.png`

### 2.3 危害实证

**机密性（实测：高）** —— 用匿名创建的账户登录控制台换取 JWT，直接读出生产配置明文：

```yaml
spring:
  datasource:
    url: jdbc:mysql://10.10.10.21:3306/paydb
    username: payapp
    password: Pr0d_DB_P@ssw0rd!
  redis:
    password: R3dis_Pr0d_Pwd
app:
  hmac-key: 8f2a1c9e5b7d4a6f0e3c1b8d7a5f2e9c
```

同一 JWT 同时打通了 ADMIN API（`GET /v3/admin/core/namespace/list` 返回 200），说明 `nacos.core.auth.admin.enabled=true` 对这条路径没有任何防御价值——攻击者拿的是"合法账号"，不是在绕过 admin 过滤器。

另外，`GET /v3/auth/user/list` 匿名即可拿到全部用户的 bcrypt 哈希，可离线爆破，也是横向移动的素材。

**完整性（实测：高）** —— 用同一个 JWT 改写生产配置成功（`{"code":0,"data":true}`），回读确认数据库地址已被指向攻击者控制的主机。Nacos 客户端通过长轮询/gRPC 秒级推送变更，这意味着配置投毒会在极短时间内扩散到全部订阅该配置的微服务实例。

**可用性（实测：高）** —— `DELETE /v3/auth/user` 与 `DELETE /v3/auth/role` 均匿名放行且实测生效（创建 `victim_demo` → 列表可见 → 匿名删除 → 列表消失）。攻击者可匿名摘掉真实管理员的 `ROLE_ADMIN` 绑定或直接删除管理员账号，把管理面锁死。

**截图：** `13-步骤3-机密性危害.png`、`14-步骤4-完整性与可用性危害.png`、`09-攻击者账号读取生产敏感配置.png`、`10-配置被投毒-管理员视角.png`

### 2.4 受影响接口范围（实测矩阵）

在 8848 端口对 `/v3/auth/**` 逐个探测，**11 个接口中 10 个匿名放行**：

| 方法 | 路径 | 匿名状态码 |
| --- | --- | --- |
| GET | /v3/auth/user/list | 200 |
| GET | /v3/auth/user/search | 200 |
| POST | /v3/auth/user | 200 |
| DELETE | /v3/auth/user | 200 |
| **PUT** | **/v3/auth/user** | **403** |
| GET | /v3/auth/role/list | 200 |
| POST | /v3/auth/role | 200 |
| DELETE | /v3/auth/role | 200 |
| GET | /v3/auth/permission/list | 200 |
| POST | /v3/auth/permission | 200 |
| DELETE | /v3/auth/permission | 200 |

唯一被拦下的 `PUT /v3/auth/user`（改密）并不是因为它的 `apiType` 写对了——3.2.3 里它同样没写 `apiType`——而是因为它额外带了 `ONLY_IDENTITY` 标签强制做身份校验。换句话说，它是**碰巧**没中招。

**截图：** `15-步骤5-匿名可达接口矩阵.png`

---

## 3. 根因分析（基于官方源码，已核对）

Nacos 3.x 把 HTTP 鉴权按 API 作用域拆成三个独立过滤器，各自有独立开关：

| 作用域 | 过滤器 | 覆盖路径 | 出厂默认 |
| --- | --- | --- | --- |
| `OPEN_API` | AuthFilter | SDK / OpenAPI | **关闭** |
| `ADMIN_API` | AuthAdminFilter | `/v3/admin/**` | 开启 |
| `CONSOLE_API` | NacosConsoleAuthFilter | `/v3/console/**` | 开启 |

而 `@Secured` 注解的作用域缺省值，恰好是那个默认不鉴权的桶（`auth/.../Secured.java`）：

```java
ApiType apiType() default ApiType.OPEN_API;
```

3.2.3 中 `UserControllerV3` / `RoleControllerV3` / `PermissionControllerV3` 的全部 `@Secured` 都只声明了 `resource` 和 `action`：

```java
@Secured(resource = AuthConstants.CONSOLE_RESOURCE_NAME_PREFIX + "users",
    action = ActionTypes.WRITE)          // 没有 apiType → 落入 OPEN_API 桶
@PostMapping
public Result<String> createUser(...)
```

注解写的是 `console/users` 这样的控制台资源名，语义上明摆着是控制台管理接口，但作用域却被缺省成了 OPEN_API。于是"管理员开了 admin+console 双面认证"和"这三个管理接口对匿名完全放行"可以同时成立——这正是这个洞最具欺骗性的地方。

官方 3.2.4 的修复方式印证了这一判断：给这三个 Controller 的**每一个** `@Secured` 逐个补上 `apiType = ApiType.ADMIN_API`（共 13 处），没有改动任何业务逻辑。

```diff
- action = ActionTypes.WRITE)
+ action = ActionTypes.WRITE, apiType = ApiType.ADMIN_API)
```

权限值 `resource=*` 在服务端经正则化后可匹配全部资源，因此单条 `*` + `rw` 权限即等价于全局读写。

**截图：** `16-步骤6-根因与官方补丁.png`

### 3.1 附带发现：状态接口的误导性

这一条是本次验证中发现的、原始情报未提及的问题，已核对源码：

`core/.../AuthModuleStateBuilder.java` 中，对外汇报的 `auth_enabled` 字段是 OPEN_API 与 ADMIN 两个作用域的**逻辑或**：

```java
boolean authEnabled = NacosAuthConfigHolder.getInstance()
    .isAnyAuthEnabled(NACOS_SERVER_AUTH_SCOPE, NACOS_SERVER_ADMIN_AUTH_SCOPE);
```

实测在 `nacos.core.auth.enabled=false` 的受影响配置下，`/v3/console/server/state` 依然返回 `"auth_enabled":"true"`。

这意味着：**任何依赖该字段做资产核查的自动化脚本或巡检平台，都会把受影响实例判为"已开启鉴权"而漏报。** 排查必须落到 `conf/application.properties` 的 `nacos.core.auth.enabled` 上，或者直接用 `curl` 打一次 8848 的 `/nacos/v3/auth/user/list` 看是否 200。

---

## 4. 危害研判

以下为基于实测结果的分析研判，非实测本身。

### 4.1 与原始情报评级的差异

原情报（评估时间 2026-08-27）给出 CVSS 9.3，机密性"高"、**完整性"低"**、可用性"高"。本次实测认为**完整性应为"高"**：攻击者获得的是 `resource=*` + `action=rw` 的全局读写权限，实测可任意改写生产配置且回读确认生效。这里"低"的评分与实际能力不符，倾向于是评分录入偏差。这是本报告与原情报唯一实质性分歧点。

### 4.2 实际攻击价值

Nacos 在典型微服务架构中同时是注册中心和配置中心，横在整个服务拓扑的中枢位置。拿下 Nacos 的真实收益不在 Nacos 本身：

1. **凭证集散地** —— 配置中心里躺着数据库口令、Redis/MQ 口令、对象存储 AK/SK、第三方 API 密钥、JWT/HMAC 签名密钥。一次读取即完成大范围凭证收割，后续横向移动不再需要碰 Nacos。实测中一份配置就同时泄露了 MySQL 口令、Redis 口令和 HMAC 密钥。
2. **配置投毒 → 供应链式扩散** —— 改一条配置，全部订阅方秒级生效。可行的方向包括：把数据源指向攻击者控制的库以劫持业务数据流；关闭下游的鉴权/风控开关；投毒日志或监控上报地址。**（此处为推断：能否进一步升级为代码执行取决于下游客户端框架如何消费配置，且发生位置是订阅方应用进程，与 Nacos 服务器本机无关；本次未做实证，详见 4.5。）**
3. **注册中心劫持** —— 全局写权限同样覆盖服务实例注册，可篡改服务实例地址实现流量劫持与中间人。**（推断，本次未实证。）**
4. **管理面锁死** —— 匿名删除管理员账号或其角色绑定，实测可行，是应急响应期间的现实风险。

### 4.3 利用门槛

极低，且适合自动化批量扫描：无需凭证、无需绕过任何安全机制、无需受害者配合、出厂默认配置即可利用；请求全部是常规 HTTP GET/POST/DELETE，不含畸形报文，WAF 基本无从识别。唯一的时间成本是 15 秒权限缓存窗口。

### 4.4 隐蔽性与检测

利用行为会在库里留下明确痕迹：多出一个用户、一个角色、一条 `*`/`rw` 权限。但存在两个现实问题：

- **痕迹容易被清理** —— `DELETE /v3/auth/user|role|permission` 同样匿名可用，攻击者在拿到 JWT 后可以立刻删掉自建账号，而 JWT 默认有效期 18000 秒（5 小时），删号不影响已签发 token 继续使用。这使得"登录后台看有没有可疑账号"这一排查方法存在明显的时间窗口盲区。
- **状态接口误导** —— 见 3.1，自动化资产核查会漏报。

因此排查建议按以下优先级：

1. 直接探测：`curl -s -o /dev/null -w '%{http_code}' http://<host>:8848/nacos/v3/auth/user/list?pageNo=1&pageSize=1`，返回 200 即为受影响。
2. 核对 `conf/application.properties` 中 `nacos.core.auth.enabled` 的实际取值，**不要**依赖 `/v3/console/server/state` 的 `auth_enabled` 字段。
3. 排查用户/角色/权限表中的异常条目，重点是 `resource=*` 的权限记录。
4. 检索访问日志中所有指向 `/v3/auth/user`、`/v3/auth/role`、`/v3/auth/permission` 的 POST/DELETE 请求——正常运维流量不会走 8848 的这几个路径，命中即高度可疑。日志留存期短于攻击窗口的，应视为无法排除。

---

### 4.5 影响边界：不涉及操作系统层面接管

需要明确划清一条边界。该漏洞是应用层的鉴权作用域缺陷，攻击者最终拿到的是 Nacos 自身 RBAC 体系里的一个账号和一条全局读写权限，能力范围止于 Nacos 的 HTTP API 与控制台，**不构成对 Nacos 所在主机的接管**。整条利用链的每一步——创建用户、绑定角色、授予权限、登录取 JWT、读写配置——调用的都是 Nacos 正常的业务接口，链路中不存在反序列化、表达式/模板注入、文件上传或路径穿越这类可导向命令执行的原语。本次验证中也未获得任何操作系统层面的能力：没有命令执行，没有宿主机文件读写，没有反弹 shell。因此就 Nacos 服务器本机而言，不需要按"主机失陷"的级别启动应急，处置重心应放在配置数据的机密性与完整性上。

这里说的"无操作系统风险"，限定含义是**漏洞本身不提供任何 OS 执行原语**。它不等于后续没有主机被攻陷的可能：配置中心里存放的数据库口令、云 AK/SK、SSH 私钥等凭证一旦被读走，攻击者可以拿这些凭证去登录**其他**系统。但那条路径是"合法凭证登录别的系统"，属于凭证泄露的衍生风险，与"这个漏洞给了 OS 权限"是两回事，在定级和处置上应当分开计算。

## 5. 处置建议

**根治（首选）：** 升级至 3.2.4 及以上。

**无法立即升级时：**

1. **网络收敛（立即可做，零业务影响）** —— 8848/9848/9849 与控制台端口限制到可信网段。Nacos 属内网设施，公网暴露应无条件回撤。这是唯一一个既能立刻止血又不影响业务的措施。
2. **开启 OPEN_API 鉴权** —— `nacos.core.auth.enabled=true`，并配置集群内一致的 `token.secret.key`。**代价明确：所有 SDK 客户端必须改造为携带凭证，否则会大面积拉取配置失败。** 必须先与业务方确认并灰度，不要在没有回滚预案的情况下直接推全量。
3. **凭证轮换** —— 若实例曾暴露在不可信网络，应假定配置中心内的凭证已泄露，按泄露处置流程轮换数据库口令、密钥等，而不是只打补丁。

---

## 6. 附件

| 目录 | 内容 |
| --- | --- |
| `evidence/screenshots/` | 全流程截图 17 张（编号 00–16） |
| `evidence/logs/` | 各步骤原始输出（`step0`–`step6`） |
| `poc/` | 本次使用的 PoC（Python / Go 两份） |

### 截图索引

| 编号 | 内容 |
| --- | --- |
| 00 | 受影响环境确认：版本 3.2.3 + 鉴权开关默认值 |
| 01–04 | 攻击前管理员视角：用户列表、权限列表、配置列表、敏感配置内容 |
| 05–07 | 攻击后管理员视角：攻击者账号、角色绑定、`*`/读写 权限 |
| 08–09 | 攻击者账号登录控制台并读取生产敏感配置 |
| 10 | 配置被投毒后的管理员视角 |
| 11 | 步骤 1：攻击前基线（8080 403 / 8848 200） |
| 12 | 步骤 2：PoC 执行全过程 |
| 13 | 步骤 3：机密性危害实证 |
| 14 | 步骤 4：完整性与可用性危害实证 |
| 15 | 步骤 5：`/v3/auth/**` 匿名可达性矩阵 |
| 16 | 步骤 6：根因定位与官方 3.2.4 补丁 |

---

## 来源说明

| 内容 | 来源与层级 |
| --- | --- |
| 漏洞编号、影响范围、评级、缓解措施 | 微步在线漏洞情报报告（XVE-2026-53002），机构二级来源 |
| `@Secured` 注解、三个 Controller、`AuthModuleStateBuilder` 源码及 3.2.3→3.2.4 差异 | alibaba/nacos 官方仓库 tag `3.2.3` / `3.2.4`，一级来源，已本地核对 |
| 修复版本 3.2.4 | Nacos 官方 Release，一级来源 |
| 全部实测数据、HTTP 状态码、接口矩阵、截图 | 本次本地受控环境实际执行 |
| 4.2 节中标注"推断"的 RCE 链条与注册中心劫持 | 分析推断，本次未实证 |

**免责声明：** 本报告全部操作在本地隔离环境（127.0.0.1）完成，仅用于授权范围内的安全验证。
